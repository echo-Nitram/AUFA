name: CI

on:
  # Work on a branch is checked through its pull request. Restricting push to the
  # integration branch avoids running the whole workflow twice for every commit
  # on a branch that already has one open. Update this if the branch is renamed.
  push:
    branches: ['claude/aufa-platform-architecture-u0kNJ']
  pull_request:

# A newer push on the same branch makes the previous run irrelevant.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: Typecheck and tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Install dependencies
        run: npm ci

      # The API's types come from the generated Prisma client, so nothing
      # typechecks until this runs.
      - name: Generate Prisma client
        run: npm run db:generate

      - name: Typecheck
        run: npm run typecheck

      - name: Tests
        run: npm test
